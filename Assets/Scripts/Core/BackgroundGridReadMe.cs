using UnityEngine;

public class BackgroundGridReadMe : MonoBehaviour
{
    /*
     * Adjust the size of the plane using X and Z Scale.  It's
     * default is 1000 x 1000.
     * Adjust the density of the grid by adjusting the Tiling
     * values of the material.  The default is 250 x 250.
     * If you want a smooth boarder, the size of the plane should
     * be a multiple of the tiling values.
    */
}
